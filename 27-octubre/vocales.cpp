#include <iostream>

using namespace std;

int main (){
	const int TAM=10;
	char vocales[TAM]="123456790";
	int encA=0, encB=0, encC=0, encD=0, encE=0;
	for(int cont=0 ; cont<TAM ; cont++){
		if(vocales[TAM]=''){
			encA++;
		} else {
			
		}
			if(vocales[TAM]='E' && vocales[TAM]='e'){
				encE++;
			}
				if(vocales[TAM]='' && vocales[TAM]=''){
					encC++;
				}
					if(vocales[TAM]='' && vocales[TAM]=''){
						encD++;
					}
						if(vocales[TAM]='E' && vocales[TAM]='e'){
							encE++;
						}

	}

	cout << "Encontre " << encontradas << " vocales";
}
